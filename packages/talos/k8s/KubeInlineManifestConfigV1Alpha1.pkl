/// KubeInlineManifestConfig configures a Kubernetes manifest to be applied to the cluster.
///
/// This module was generated from JSON Schema from
/// <https://raw.githubusercontent.com/siderolabs/talos/v1.14.0/pkg/machinery/config/schemas/config.schema.json>.
module k8s.KubeInlineManifestConfigV1Alpha1

extends ".../TalosDocument.pkl"

apiVersion = "v1alpha1"

kind = "KubeInlineManifestConfig"

/// name
///
/// Name of manifest.
name: String

/// manifest
///
/// Kubernetes manifest definition, it is supplied as a raw string. It might contain a set of YAML
/// documents separated by ---. The format matches what can be supplied as kubectl apply -f &lt;file&gt;.
manifest: String
