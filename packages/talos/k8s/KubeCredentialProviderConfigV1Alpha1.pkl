/// KubeCredentialProviderConfig configures kubelet's credential provider.
///
/// This module was generated from JSON Schema from
/// <https://raw.githubusercontent.com/siderolabs/talos/v1.14.0/pkg/machinery/config/schemas/config.schema.json>.
module k8s.KubeCredentialProviderConfigV1Alpha1

extends ".../TalosDocument.pkl"

apiVersion = "v1alpha1"

kind = "KubeCredentialProviderConfig"

/// configuration
///
/// Kubelet credential provider configuration (used for image registry authentication). The value is the
/// literal kubelet’s credential provider configuration.
configuration: Dynamic?
