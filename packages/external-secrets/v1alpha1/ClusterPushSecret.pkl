/// ClusterPushSecret is the Schema for the ClusterPushSecrets API that enables cluster-wide management
/// of pushing Kubernetes secrets to external providers.
///
/// This module was generated from the CustomResourceDefinition at
/// <https://raw.githubusercontent.com/external-secrets/external-secrets/v2.10.0/deploy/crds/bundle.yaml>.
module io.`external-secrets`.v1alpha1.ClusterPushSecret

extends "@k8s/K8sResource.pkl"

import "@k8s/apimachinery/pkg/apis/meta/v1/ObjectMeta.pkl"

fixed apiVersion: "external-secrets.io/v1alpha1"

fixed kind: "ClusterPushSecret"

/// Standard object's metadata.
///
/// More info: <https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata>.
metadata: ObjectMeta?

/// ClusterPushSecretSpec defines the configuration for a ClusterPushSecret resource.
spec: Spec?

/// ClusterPushSecretStatus contains the status information for the ClusterPushSecret resource.
status: Status?

/// ClusterPushSecretSpec defines the configuration for a ClusterPushSecret resource.
class Spec {
  /// A list of labels to select by to find the Namespaces to create the ExternalSecrets in. The
  /// selectors are ORed.
  namespaceSelectors: Listing<NamespaceSelector>?

  /// The metadata of the external secrets to be created
  pushSecretMetadata: PushSecretMetadata?

  /// The name of the push secrets to be created. Defaults to the name of the ClusterPushSecret
  pushSecretName: String(length.isBetween(1, 253), matches(Regex(#"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"#)))?

  /// PushSecretSpec defines what to do with the secrets.
  pushSecretSpec: PushSecretSpec

  /// The time in which the controller should reconcile its objects and recheck namespaces for labels.
  refreshTime: String?
}

/// A label selector is a label query over a set of resources. The result of matchLabels and
/// matchExpressions are ANDed. An empty label selector matches all objects. A null label selector
/// matches no objects.
class NamespaceSelector {
  /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
  matchExpressions: Listing<SelectorMatchExpression>?

  /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is
  /// equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and
  /// the values array contains only "value". The requirements are ANDed.
  matchLabels: Mapping<String, String>?
}

/// A label selector requirement is a selector that contains values, a key, and an operator that relates
/// the key and values.
class SelectorMatchExpression {
  /// key is the label key that the selector applies to.
  key: String

  /// operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists
  /// and DoesNotExist.
  operator: String

  /// values is an array of string values. If the operator is In or NotIn, the values array must be
  /// non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is
  /// replaced during a strategic merge patch.
  values: Listing<String>?
}

/// The metadata of the external secrets to be created
class PushSecretMetadata {
  annotations: Mapping<String, String>?

  labels: Mapping<String, String>?
}

/// PushSecretSpec defines what to do with the secrets.
class PushSecretSpec {
  /// Secret Data that should be pushed to providers
  data: Listing<Datum>?

  /// DataTo defines bulk push rules that expand source Secret keys into provider entries.
  dataTo: Listing<DataTo>?

  /// Deletion Policy to handle Secrets in the provider.
  ///
  /// Default if undefined: `"None"`
  deletionPolicy: ("Delete"|"None")?

  /// The Interval to which External Secrets will try to push a secret definition
  ///
  /// Default if undefined: `"1h0m0s"`
  refreshInterval: String?

  secretStoreRefs: Listing<SecretStoreRef>

  /// The Secret Selector (k8s source) for the Push Secret
  selector: Selector

  /// Template defines a blueprint for the created Secret resource.
  template: Template?

  /// UpdatePolicy to handle Secrets in the provider.
  ///
  /// Default if undefined: `"Replace"`
  updatePolicy: ("Replace"|"IfNotExists")?
}

/// PushSecretData defines data to be pushed to the provider and associated metadata.
class Datum {
  /// Used to define a conversion Strategy for the secret keys
  ///
  /// Default if undefined: `"None"`
  conversionStrategy: ("None"|"ReverseUnicode")?

  /// Match a given Secret Key to be pushed to the provider.
  match: Match

  /// Metadata is metadata attached to the secret. The structure of metadata is provider specific, please
  /// look it up in the provider documentation.
  metadata: Null?
}

/// Match a given Secret Key to be pushed to the provider.
class Match {
  /// Remote Refs to push to providers.
  remoteRef: RemoteRef

  /// Secret Key to be pushed
  secretKey: String?
}

/// Remote Refs to push to providers.
class RemoteRef {
  /// Name of the property in the resulting secret
  property: String?

  /// Name of the resulting provider secret.
  remoteKey: String
}

/// PushSecretDataTo defines how to bulk-push secrets to providers without explicit per-key mappings.
class DataTo {
  /// Used to define a conversion Strategy for the secret keys
  ///
  /// Default if undefined: `"None"`
  conversionStrategy: ("None"|"ReverseUnicode")?

  /// Match pattern for selecting keys from the source Secret. If not specified, all keys are selected.
  match: DataToMatch?

  /// Metadata is metadata attached to the secret. The structure of metadata is provider specific, please
  /// look it up in the provider documentation.
  metadata: Null?

  /// RemoteKey is the name of the single provider secret that will receive ALL matched keys bundled as a
  /// JSON object (e.g. {"DB_HOST":"...","DB_USER":"..."}). When set, per-key expansion is skipped and a
  /// single push is performed. The provider's store prefix (if any) is still prepended to this value.
  /// When not set, each matched key is pushed as its own individual provider secret.
  remoteKey: String?

  /// Rewrite operations to transform keys before pushing to the provider. Operations are applied
  /// sequentially.
  rewrite: Listing<Rewrite>?

  /// StoreRef specifies which SecretStore to push to. Required.
  storeRef: StoreRef?
}

/// Match pattern for selecting keys from the source Secret. If not specified, all keys are selected.
class DataToMatch {
  /// Regexp matches keys by regular expression. If not specified, all keys are matched.
  regexp: String?
}

/// PushSecretRewrite defines how to transform secret keys before pushing.
class Rewrite {
  /// Used to rewrite with regular expressions.
  regexp: Regexp?

  /// Used to apply string transformation on the secrets.
  transform: Transform?
}

/// Used to rewrite with regular expressions.
class Regexp {
  /// Used to define the regular expression of a re.Compiler.
  source: String

  /// Used to define the target pattern of a ReplaceAll operation.
  target: String
}

/// Used to apply string transformation on the secrets.
class Transform {
  /// Used to define the template to apply on the secret name. `.value ` will specify the secret name in
  /// the template.
  template: String
}

/// StoreRef specifies which SecretStore to push to. Required.
class StoreRef {
  /// Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
  ///
  /// Default if undefined: `"SecretStore"`
  kind: ("SecretStore"|"ClusterSecretStore")?

  /// Optionally, sync to secret stores with label selector
  labelSelector: SecretStoreRefLabelSelector?

  /// Optionally, sync to the SecretStore of the given name
  name: String(length.isBetween(1, 253), matches(Regex(#"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"#)))?
}

/// Optionally, sync to secret stores with label selector
class SecretStoreRefLabelSelector {
  /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
  matchExpressions: Listing<SelectorMatchExpression>?

  /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is
  /// equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and
  /// the values array contains only "value". The requirements are ANDed.
  matchLabels: Mapping<String, String>?
}

/// PushSecretStoreRef contains a reference on how to sync to a SecretStore.
class SecretStoreRef {
  /// Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
  ///
  /// Default if undefined: `"SecretStore"`
  kind: ("SecretStore"|"ClusterSecretStore")?

  /// Optionally, sync to secret stores with label selector
  labelSelector: SecretStoreRefLabelSelector?

  /// Optionally, sync to the SecretStore of the given name
  name: String(length.isBetween(1, 253), matches(Regex(#"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"#)))?
}

/// The Secret Selector (k8s source) for the Push Secret
class Selector {
  /// Point to a generator to create a Secret.
  generatorRef: GeneratorRef?

  /// Select a Secret to Push.
  secret: Secret?
}

/// Point to a generator to create a Secret.
class GeneratorRef {
  /// Specify the apiVersion of the generator resource
  ///
  /// Default if undefined: `"generators.external-secrets.io/v1alpha1"`
  apiVersion: String?

  /// Specify the Kind of the generator resource
  kind: 
    "ACRAccessToken"
    |"BeyondtrustWorkloadCredentialsDynamicSecret"
    |"ClusterGenerator"
    |"CloudsmithAccessToken"
    |"ECRAuthorizationToken"
    |"Fake"
    |"GCRAccessToken"
    |"GithubAccessToken"
    |"GitlabDeployToken"
    |"QuayAccessToken"
    |"Password"
    |"SSHKey"
    |"STSSessionToken"
    |"UUID"
    |"VaultDynamicSecret"
    |"Webhook"
    |"Grafana"
    |"MFA"

  /// Specify the name of the generator resource
  name: String(length.isBetween(1, 253), matches(Regex(#"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"#)))
}

/// Select a Secret to Push.
class Secret {
  /// Name of the Secret. The Secret must exist in the same namespace as the PushSecret manifest.
  name: String(length.isBetween(1, 253), matches(Regex(#"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"#)))?

  /// Selector chooses secrets using a labelSelector.
  selector: SecretSelector?
}

/// Selector chooses secrets using a labelSelector.
class SecretSelector {
  /// matchExpressions is a list of label selector requirements. The requirements are ANDed.
  matchExpressions: Listing<SelectorMatchExpression>?

  /// matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is
  /// equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and
  /// the values array contains only "value". The requirements are ANDed.
  matchLabels: Mapping<String, String>?
}

/// Template defines a blueprint for the created Secret resource.
class Template {
  data: Mapping<String, String>?

  /// EngineVersion specifies the template engine version that should be used to compile/execute the
  /// template specified in .data and .templateFrom[].
  ///
  /// Default if undefined: `"v2"`
  engineVersion: "v2"?

  /// TemplateMergePolicy defines how the rendered template should be merged with the existing Secret
  /// data.
  ///
  /// Default if undefined: `"Replace"`
  mergePolicy: ("Replace"|"Merge")?

  /// ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
  metadata: Metadata?

  templateFrom: Listing<TemplateFrom>?

  type: String?
}

/// ExternalSecretTemplateMetadata defines metadata fields for the Secret blueprint.
class Metadata {
  annotations: Mapping<String, String>?

  finalizers: Listing<String>?

  labels: Mapping<String, String>?
}

/// TemplateFrom specifies a source for templates. Each item in the list can either reference a ConfigMap
/// or a Secret resource.
class TemplateFrom {
  /// TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
  configMap: TemplateFromSecret?

  literal: String?

  /// TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
  secret: TemplateFromSecret?

  /// Target specifies where to place the template result. For Secret resources the accepted values are
  /// empty, "Data", "Annotations" and "Labels"; any other value is rejected because it would allow
  /// writes to privileged Secret fields. For custom resources (when spec.target.manifest is set), this
  /// supports nested paths like "spec.database.config" or "data".
  ///
  /// Default if undefined: `"Data"`
  target: String?

  /// Used to define a decoding Strategy for the rendered template values. Defaults to None when omitted.
  valuesDecodingStrategy: ("Auto"|"Base64"|"Base64URL"|"None")?
}

/// TemplateRef specifies a reference to either a ConfigMap or a Secret resource.
class TemplateFromSecret {
  /// A list of keys in the ConfigMap/Secret to use as templates for Secret data
  items: Listing<SecretItem>

  /// The name of the ConfigMap/Secret resource
  name: String(length.isBetween(1, 253), matches(Regex(#"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$"#)))
}

/// TemplateRefItem specifies a key in the ConfigMap/Secret to use as a template for Secret data.
class SecretItem {
  /// A key in the ConfigMap/Secret
  key: String(length.isBetween(1, 253), matches(Regex("^[-._a-zA-Z0-9]+$")))

  /// TemplateScope specifies how the template keys should be interpreted.
  ///
  /// Default if undefined: `"Values"`
  templateAs: ("Values"|"KeysAndValues")?
}

/// ClusterPushSecretStatus contains the status information for the ClusterPushSecret resource.
class Status {
  conditions: Listing<Condition>?

  /// Failed namespaces are the namespaces that failed to apply an PushSecret
  failedNamespaces: Listing<FailedNamespace>?

  /// ProvisionedNamespaces are the namespaces where the ClusterPushSecret has secrets
  provisionedNamespaces: Listing<String>?

  pushSecretName: String?
}

/// PushSecretStatusCondition indicates the status of the PushSecret.
class Condition {
  lastTransitionTime: String?

  message: String?

  reason: String?

  status: String

  /// PushSecretConditionType indicates the condition of the PushSecret.
  type: String
}

/// ClusterPushSecretNamespaceFailure represents a failed namespace deployment and it's reason.
class FailedNamespace {
  /// Namespace is the namespace that failed when trying to apply an PushSecret
  namespace: String

  /// Reason is why the PushSecret failed to apply to the namespace
  reason: String?
}
