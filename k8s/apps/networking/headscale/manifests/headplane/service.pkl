amends "@k8s/api/core/v1/Service.pkl"

import ".../app.pkl"

metadata {
  name = "headplane"
}

spec {
  type = "ClusterIP"
  selector {
    ["app"] = app.headplaneAppLabel
  }
  ports {
    new {
      port = 3000
      targetPort = 3000
      protocol = "TCP"
    }
  }
}
